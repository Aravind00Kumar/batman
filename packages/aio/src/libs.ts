// Enable it for production
export {
    BaseComponent, ProjectorOptions, VNode, h, hParser, ProjectorFactory, IWriter, IMessage
} from '@batman/core/core';

export {
    Doughnut, IDoughnutComponent, IDoughnutOptions,
    List, IListComponent, IListOptions,
    Tree, OptimalTree, ITreeComponent, IOptimalTreeOptions, IOptimalTreeNode,
} from '@batman/components/components';

// Enable it for debugging
// export {
//     BaseComponent, ProjectorOptions, VNode, h, hParser, ProjectorFactory, IWriter, IMessage
// } from '../../core/src/core';
